<resources>

    <!-- root themes -->
    <style name="AppTheme" parent="AppTheme.Base" />
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- base application theme -->
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowAnimationStyle">@style/MyAnimation.Window</item>
        <item name="colorControlNormal">@android:color/white</item>
        <item name="android:homeAsUpIndicator">@drawable/ic_arrow_back</item>
    </style>

    <!-- base application theme full screen -->
    <style name="AppThemeFull" parent="AppTheme.Base">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- base application theme full screen without actionbar-->
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Animations for a non-full-screen window or activity. -->
    <style name="MyAnimation.Window" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/fadein</item>
        <item name="android:windowExitAnimation">@anim/fadeout</item>
    </style>

    <!-- style for text input -->
    <style name="TextInputLayout" parent="AppTheme.Base">
        <item name="android:textColorHint">@android:color/white</item>
        <item name="colorAccent">@android:color/white</item>
        <item name="colorControlNormal">@android:color/white</item>
        <item name="colorControlActivated">@android:color/white</item>
        <item name="android:textSize">@dimen/text_size</item>
    </style>

    <!-- style for edit text inactive (only for read value)-->
    <style name="EditTextReadOnly" parent="AppTheme.Base">
        <item name="colorControlActivated">@android:color/transparent</item>
        <item name="android:textSize">@dimen/text_size</item>
        <item name="android:focusable">false</item>
        <item name="android:editable">false</item>
        <item name="android:clickable">false</item>
        <item name="android:inputType">none</item>
        <item name="android:focusableInTouchMode">false</item>
        <!--
        <item name="colorAccent">@android:color/holo_red_dark</item>
        <item name="colorControlNormal">@android:color/holo_green_dark</item>
        <item name="colorControlHighlight">@android:color/holo_purple</item>
        -->
    </style>

    <!-- style for edit text active -->
    <style name="EditText" parent="AppTheme.Base">
        <item name="colorControlActivated">@android:color/white</item>
        <item name="colorAccent">@android:color/holo_red_dark</item>
        <item name="colorControlNormal">@android:color/white</item>
        <item name="colorControlHighlight">@android:color/white</item>
        <item name="android:textSize">@dimen/text_size_edit_text</item>
    </style>

    <!-- style for text input on white background-->
    <style name="TextInputLayoutOnWhite" parent="AppTheme.Base">
        <item name="android:textColorHint">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="colorControlNormal">@color/colorPrimary</item>
        <item name="colorControlActivated">@color/colorPrimary</item>
        <item name="android:textSize">@dimen/text_size</item>
    </style>

    <!-- base font mono bold -->
    <style name="TextAppearance_FontPathRobotoMonoBold" parent="android:TextAppearance">
        <item name="fontPath">fonts/RobotoMono-Bold.ttf</item>
        <item name="android:textColor">#666666</item>
        <item name="android:textSize">@dimen/text_size</item>
    </style>

    <!-- base font open sans bold -->
    <style name="TextAppearance_FontPathOpenSansBold" parent="android:TextAppearance">
        <item name="fontPath">fonts/OpenSans-Bold.ttf</item>
        <item name="android:textSize">@dimen/text_size</item>
    </style>

    <!-- base font open sans regular -->
    <style name="TextAppearance_FontPathOpenSansRegular" parent="android:TextAppearance">
        <item name="fontPath">fonts/OpenSans-Regular.ttf</item>
        <item name="android:textColor">#666666</item>
        <item name="android:textSize">@dimen/text_size</item>
    </style>

    <!-- toolbar title text size -->
    <style name="Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">@dimen/text_size_toolbar_title</item>
    </style>

    <!-- toolbar subtitle text size -->
    <style name="Toolbar.SubtitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">@dimen/text_size</item>
    </style>

    <!-- dialog -->
    <style name="DialogTheme" parent="android:Theme.Holo.Light.Dialog">
        <item name="android:windowMinWidthMajor">90%</item>
        <item name="android:windowMinWidthMinor">90%</item>
    </style>

</resources>